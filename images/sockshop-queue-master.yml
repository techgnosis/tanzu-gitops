#@ load("@ytt:data", "data")

apiVersion: kpack.io/v1alpha1
kind: Image
metadata:
  name: sockshop-queue-master
  namespace: images
spec:
  tag: #@ data.values.sockshop_queue_master_image
  serviceAccount: tbs-service-account
  builder:
    name: base
    kind: ClusterBuilder
  source:
    git:
      url: https://github.com/microservices-demo/queue-master.git
      revision: master

