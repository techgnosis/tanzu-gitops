#@ load("@ytt:data", "data")
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - name: demo-app
        imagePullPolicy: Always
        image: #@ data.values.harbor_domain + "/library/demo-app:latest"
        ports:
        - containerPort: 8080
        env:
        - name: DEMO_APP_DB_TYPE
          value: test